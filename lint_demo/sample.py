Here is a short, deliberately messy piece of code. Do not fix anything yet — we want it messy so the tools have things to find. You will paste it in the next step.

import os
import math

def calculate_total(price, quantity):
    discount = 10
    total = price * quantity
    return total
    print("done")

def get_status(code):
    if code == 200:
        result = "OK"
    return result

x = undefined_variable + 5
